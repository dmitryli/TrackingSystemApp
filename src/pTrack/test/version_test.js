// 'use strict';

// describe('pTrack.version module', function () {
// 	beforeEach(module('pTrack.version'));

// 	describe('version service', function () {
// 		it('should return current version', inject(function (version) {
// 			expect(version).toEqual('0.1');
// 		}));
// 	});
// });